<div class="home_container d-flex justify-content-center align-items-center">
  <h1>Edit task</h1>
  <%= render 'form' %>
  <%= link_to 'Back', user_task_path(@user, @task), class: 'btn btn-secondary' %>
</div>
